---
- hosts: web-servers
  remote_user: ansadmin
  become: true
  tasks:
  - name: copier file source vers destination
    copy:
      src: 'target/mon-appli 1.0-SNAPSHOT.jar'
      dest: '/home/ansadmin/test'
      
 
